# Apps workspace

- `apps/mobile`: Mobile client application.
- `apps/web`: Consumer web client application.
- `apps/admin`: Admin web console application.

Each app contains its own `.env.example` file that defines its required runtime contract.
