{
  "name": "home-app",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit/config.test.js tests/rules/firestore-rules.test.js tests/integration/static-server.test.js && npx firebase emulators:exec --only firestore \"vitest run tests/firestore.rules.test.js tests/repository.integration.test.js\"",
    "test:rules": "vitest run tests/firestore.rules.test.js",
    "test:integration": "vitest run tests/repository.integration.test.js",
    "test:ci": "npm run test:rules && npm run test:integration",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@playwright/test": "^1.51.1",
    "firebase": "^11.3.1",
    "firebase-tools": "^13.34.0",
    "vitest": "^3.0.5"
  }
}
